get:
  summary: "Chat"
  parameters:
    - name: "chat_id"
      in: "path"
      description: "ID of chat to return"
      required: true
      type: "integer"
  responses:
    200:
      description: "OK"
      schema:
        $ref: '/static/definitions/responses/chat.yml'
    401:
      description: "Unauthorized"

post:
  summary: "Chat"
  parameters:
  responses:
    200:
      description: "OK"
    401:
      description: "Unauthorized"

